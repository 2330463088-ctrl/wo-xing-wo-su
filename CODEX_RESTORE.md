# 我行我诉 Codex 恢复说明

这个压缩包包含“我行我诉”手机版诉讼指导 agent 截至 2026 年 8 月 26 日的最新完整源码及测试版。

## 主要入口

- `public/mobile-preview-v2.html`：当前最新手机版交互页面，包含完整诉讼流程、移动端样式、测试工具、证据来源同页选择、诉讼金额测算及文书生成逻辑。
- `server.py`：本地预览服务和 DeepSeek 后端代理，避免 API key 暴露到前端。
- `app/page.tsx`：Next/Vinext 入口，通过 iframe 展示 `mobile-preview.html`。

## 本地预览

在项目根目录运行：

```powershell
$env:DEEPSEEK_API_KEY="你的 DeepSeek API Key"
$env:PORT="8770"
python server.py
```

然后打开：

```text
http://127.0.0.1:8770/mobile-preview-v2.html
```

直接测试最新流程（带预填测试数据和任意步骤跳转工具）：

```text
http://127.0.0.1:8770/mobile-preview-v2.html?test=1&mobile=7&element=complete&auth=2
```

如果没有配置 `DEEPSEEK_API_KEY`，页面仍可运行，本地规则校验仍会工作，但 DeepSeek 联网判断不会启用。

## 当前关键功能

- 手机版单步问答流程。
- 测试模式：访问地址追加 `?test=1` 时显示测试面板。可用下拉框选择任意默认流程步骤并直接跳转继续测试，也可一键进入多笔还款页、利息填写页或直接查看典型计算结果；正式用户不显示。
- “要被告承担什么费用”页面只勾选律师费、诉讼费、保全费、保函费、公告费等类别，不要求填写金额；测试模式自动提供勾选数据。
- 诉讼阶段顶部节点进度条。
- 所有涉及证据来源的输入步骤，证据来源与输入框在同一页展示。
- 证据来源选项：借条、转账、聊天、录音、其他。
- 借款凭证采用大卡片多选：借条、欠条、借款合同、其他书面凭证；“没有书面凭证”与其他选项互斥。
- 所有金额输入框固定显示“元”，用户只填写阿拉伯数字；测试模式已预填数字测试数据。
- 当事人主体类型前置：证据收集一开始区分原告/被告是个人还是公司或个体户；个人填写姓名和身份证号，单位或个体户填写企业名称，不再误要求身份证号。
- 第一阶段顺序已调整为：主体 → 借贷事实 → 款项交付 → 金额计算 → 担保/时效 → 管辖法院。
- 款项交付判断：新增“款项如何交付”，区分转账、现金、现金加转账、不确定；现金交付提示需要收条或聊天记录等补强。
- 本地规则校验：姓名可疑格式、大陆/香港/台湾身份证号、人民币金额、已还款是否超过本金、日息/月息/年息识别。
- 被告还款金额测算：根据本金、借款日期、日息/月息/年息、逐笔已还款金额、逐笔还款时间、还款抵扣顺序、计算诉讼金额的截止时间，估算剩余本金、未付利息和合计金额。
- 已还款明细页面：使用一个年月日三列轮动选择器反复录入还款；添加后在下方用紧凑列表显示每笔“日期 + 金额”，年份范围为 2010 年至 2040 年。
- 有约定利息并发生还款时，可选择还款抵扣顺序；没有明确约定时，简算按“先抵扣利息、再抵扣本金”处理。
- 无约定利息时，资金占用利息从约定还款日开始估算；没有约定还款日时，从第一次催要时间开始估算，按 LPR 一倍作为页面参数计算。
- 担保人判断：有担保人时，简洁询问签名身份、是否写明连带责任、保证期间、是否向担保人催要、借款人是否明显无法还款，并输出是否建议列为共同被告。
- 管辖法院判断：金额测算后进入极简管辖流程；有约定管辖时直接填写约定法院并出结论，没有约定时先问是否知道被告所在地，不知道也可继续；再询问原告所在地、还款方式，不再重复原告地址/被告户籍地址旧问题。

## 恢复时注意

- 不要把 DeepSeek API key 写进前端页面。
- 不需要恢复 `.git`、`node_modules`、`__pycache__`、`outputs`、`work` 等目录。
- 如需重新安装依赖，可运行 `npm install`；但当前手机版静态预览主要依赖 `public/mobile-preview-v2.html`、`server.py` 和 `public/templates/` 中的 Word 模板。
